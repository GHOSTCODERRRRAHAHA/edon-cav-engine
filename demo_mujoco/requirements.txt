mujoco>=3.1.0
numpy>=1.26.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
aiofiles>=23.0.0
python-multipart>=0.0.6
requests>=2.31.0

# Note: On Windows, you may need to install MuJoCo separately:
# pip install mujoco
# If that fails, try: pip install mujoco-py (older version)
# Or install from: https://mujoco.readthedocs.io/en/latest/installation.html

