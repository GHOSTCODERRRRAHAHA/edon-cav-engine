## Deprecated

`edon-mago` has been merged into `edon-mag` as a single service.

- Use `edon-mag` for orchestration endpoints under `/mag/*`.
- The standalone MAGO service is no longer required for deployment.

For local development, start MAG and use `MAG_URL` with `/mag` routes.
